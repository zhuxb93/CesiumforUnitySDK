# 脱敏复核报告

## 复核结论
- 品牌词扫描：未发现残留。
- 乱码扫描：未发现残留。
- 凭证相关词扫描：发现 31 处，均为取消令牌、占位符、类名或第三方解析术语。
- 明文地址/内网地址扫描：未发现残留。

## 处理记录
- 命名空间、模块名、类名前缀已迁移为中性命名。
- 私有服务地址统一改为 https://example.com/service-endpoint 或模块说明中的占位地址。
- 商业插件、真实资源、缓存、二进制构建产物和业务壳未纳入。
- 第三方目录按原许可保留；其中公开链接和解析术语保留原样并在 NOTICES 中署名。

## 命中复核
### 品牌词
- 无。

### 凭证相关词
- VectorTileMesh/VectorTileset.cs:60:        protected override async UniTask LoadTileAsync(TileID tileID, CancellationTokenSource cancellationToken)
- VectorTileMesh/VectorTileset.cs:72:            if (cancellationToken != null)
- VectorTileMesh/VectorTileset.cs:74:                cancellationToken.Token.ThrowIfCancellationRequested();
- VectorTileMesh/VectorTileset.cs:97:                await ProcessBuildingTile(vectorData, meshCreateInfo, tileID, cancellationToken);
- VectorTileMesh/VectorTileset.cs:101:                await ProcessRoadTile(vectorData, meshCreateInfo, tileID, cancellationToken);
- VectorTileMesh/VectorTileset.cs:114:        private async UniTask ProcessBuildingTile(byte[] vectorData, MeshCreateInfo meshCreateInfo, TileID tileID, CancellationTokenSource cancellationToken)
- VectorTileMesh/VectorTileset.cs:124:                    if (cancellationToken != null)
- VectorTileMesh/VectorTileset.cs:126:                        cancellationToken.Token.ThrowIfCancellationRequested();
- VectorTileMesh/VectorTileset.cs:129:                }, cancellationToken.Token);
- VectorTileMesh/VectorTileset.cs:135:                    if (cancellationToken != null)
- VectorTileMesh/VectorTileset.cs:137:                        cancellationToken.Token.ThrowIfCancellationRequested();
- VectorTileMesh/VectorTileset.cs:140:                }, cancellationToken.Token);
- VectorTileMesh/VectorTileset.cs:190:        private async UniTask ProcessRoadTile(byte[] vectorData, MeshCreateInfo meshCreateInfo, TileID tileID, CancellationTokenSource cancellationToken)
- VectorTileMesh/VectorTileset.cs:196:                if (cancellationToken != null)
- VectorTileMesh/VectorTileset.cs:198:                    cancellationToken.Token.ThrowIfCancellationRequested();
- VectorTileMesh/VectorTileset.cs:201:            }, cancellationToken.Token);
- VectorTileMesh/VectorTileManagerBase.cs:26:        protected CancellationTokenSource cancellationTokenSource;
- VectorTileMesh/VectorTileManagerBase.cs:104:            cancellationTokenSource = new CancellationTokenSource();
- VectorTileMesh/VectorTileManagerBase.cs:123:                    await LoadTileAsync(tile, cancellationTokenSource);
- VectorTileMesh/VectorTileManagerBase.cs:157:        protected abstract UniTask LoadTileAsync(TileID tileID, CancellationTokenSource cancellationToken);
- VectorTileMesh/VectorTileManagerBase.cs:278:            cancellationTokenSource?.Cancel();
- VectorTileMesh/VectorTileManagerBase.cs:292:                cancellationTokenSource?.Cancel();
- README.md:23:4. If you use Cesium ion, provide your token through your own project configuration. This repository only uses the placeholder `YOUR_CESIUM_ION_TOKEN` and does not include credentials.
- README.zh-CN.md:23:4. 如需使用 Cesium ion，请在自己的项目配置中提供 token。示例只使用 `YOUR_CESIUM_ION_TOKEN` 占位，不内置任何密钥。
- CameraKeyframePlayer/CameraLerpVisual.cs:84:                    uniTaskToken?.Cancel();
- CameraKeyframePlayer/CameraLerpVisual.cs:85:                    uniTaskToken = new CancellationTokenSource();
- CameraKeyframePlayer/CameraLerpVisual.cs:95:        private CancellationTokenSource uniTaskToken;
- CameraKeyframePlayer/CameraLerpVisual.cs:102:            if (uniTaskToken != null)
- CameraKeyframePlayer/CameraLerpVisual.cs:104:                uniTaskToken.Token.ThrowIfCancellationRequested();
- CameraKeyframePlayer/CameraLerpVisual.cs:128:            if (uniTaskToken != null)
- CameraKeyframePlayer/CameraLerpVisual.cs:130:                uniTaskToken.Token.ThrowIfCancellationRequested();

### 明文/内网地址
- 无。

## 编译/导入验证
- 结构校验：`package.json` 与 `GeoTiles.asmdef` 已通过 JSON 解析。
- 本机检测到 Unity 2022.3.2f1c1，但未创建临时 Unity 项目做完整导入编译；该仓还需要目标项目预先安装 CesiumForUnity，完整编辑器导入请在具备 CesiumForUnity 的项目中复核。
- 当前已完成源码组织、依赖声明、敏感词复核和 git 提交。
